<UserControl x:Class="ApplicationSuite.WindowModules.UtilityTools.TestStringOperation.TestStringOperationUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ApplicationSuite.WindowModules.UtilityTools.TestStringOperation"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- TextBox（上部） -->
        <TextBox Grid.Row="0"
             Text="{Binding SomeText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
             FontSize="16"
             Margin="0 0 0 10"
             Padding="6"
             BorderThickness="1"
             BorderBrush="#FFCCCCCC"
             Background="#FFF"
             HorizontalAlignment="Stretch"/>

        <!-- ListBox（下部） -->
        <ListBox Grid.Row="1"
             ItemsSource="{Binding OperationItems}"
             SelectedItem="{Binding SelectedOperation, Mode=TwoWay}"
             ScrollViewer.HorizontalScrollBarVisibility="Auto"
             BorderThickness="0"
             HorizontalContentAlignment="Center">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border x:Name="ButtonSurface"
                        Background="#FFEFEFEF"
                        Padding="8"
                        Margin="4"
                        CornerRadius="4"
                        BorderBrush="#FFCCCCCC"
                        BorderThickness="1"
                        HorizontalAlignment="Center">
                        <TextBlock Text="{Binding DisplayText}"
                               FontSize="16"
                               TextAlignment="Center"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
            <ListBox.ItemContainerStyle>
                <Style TargetType="ListBoxItem">
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                    <Setter Property="VerticalContentAlignment" Value="Center"/>
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ListBoxItem">
                                <Grid>
                                    <ContentPresenter HorizontalAlignment="Center"
                                                  VerticalAlignment="Center"/>
                                </Grid>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                </Style>
            </ListBox.ItemContainerStyle>
        </ListBox>
    </Grid>


</UserControl>