<UserControl x:Class="ApplicationSuite.WindowModules.UtilityTools.LineReplaceRegex.LineReplaceRegexUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- 入力 -->
            <RowDefinition Height="Auto"/>
            <!-- Find/Replace -->
            <RowDefinition Height="Auto"/>
            <!-- Options -->
            <RowDefinition Height="Auto"/>
            <!-- 操作 -->
            <RowDefinition Height="*"/>
            <!-- 出力 -->
        </Grid.RowDefinitions>

        <!-- 入力 -->
        <TextBox Grid.Row="0" Height="120"
                 Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>

        <!-- Find / Replace -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,8,0,4">
            <TextBlock Text="Pattern:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Width="220" Text="{Binding FindText, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,16,0"/>
            <TextBlock Text="Replace:" VerticalAlignment="Center" Margin="0,0,8,0"/>
            <TextBox Width="220" Text="{Binding ReplaceText, UpdateSourceTrigger=PropertyChanged}"/>
        </StackPanel>

        <!-- Options -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,8">
            <CheckBox Content="IgnoreCase" IsChecked="{Binding IgnoreCase}"/>
            <CheckBox Content="Multiline" IsChecked="{Binding UseMultiline}" Margin="12,0,0,0"/>
        </StackPanel>

        <!-- 操作 + 初期化 -->
        <ListBox Grid.Row="3"
                 ItemsSource="{Binding OperationItems}"
                 SelectedItem="{Binding SelectedOperation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                 BorderThickness="0" Background="Transparent"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel Orientation="Horizontal"/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Margin="0,0,8,0" Padding="6" CornerRadius="6"
                            Background="#FFEEEEEE" BorderBrush="#FFCCCCCC" BorderThickness="1"
                            MinWidth="96" MinHeight="28" Cursor="Hand">
                        <TextBlock Text="{Binding DisplayText}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- 出力 -->
        <TextBox Grid.Row="4" Margin="0,8,0,0"
                 Text="{Binding OutputText, Mode=TwoWay}"
                 AcceptsReturn="True" VerticalScrollBarVisibility="Auto"/>
    </Grid>
</UserControl>
