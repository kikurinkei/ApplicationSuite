<UserControl x:Class="ApplicationSuite.WindowModules.AppShared.Utilities.WindowAction.WindowActionUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ApplicationSuite.WindowModules.AppShared.Utilities.WindowAction"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <Grid Margin="10">
        <!-- コマンド選択用のリストボックス -->
        <ListBox ItemsSource="{Binding CommandItems}"
                 SelectedItem="{Binding SelectedCommand, Mode=TwoWay}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <!-- 表示テキスト -->
                    <TextBlock Text="{Binding DisplayText}" 
                               FontSize="16"
                               Padding="4"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>
